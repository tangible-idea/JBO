{
  "name": "jev-bookmark-sorter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Chrome bookmark classifier powered by TypeSafe Jev.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server/index.mjs",
    "start": "node server/index.mjs",
    "test": "node --test",
    "check": "node --check server/index.mjs && node --check server/classifier.mjs && node --check server/metadata.mjs && node --check extension/popup.js && node --check extension/popup-utils.js && node --check extension/folder-picker.js && node --check extension/options.js && node --check extension/options-utils.js"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
